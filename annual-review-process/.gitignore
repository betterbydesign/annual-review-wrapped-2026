node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Large source screen recording — exceeds GitHub's 100MB file limit.
# Kept locally for rendering; not tracked. Use Git LFS if it must live in the repo.
public/final-presentation.mp4
